import React from 'react'

const Button = ({texto}) => {
        return (
                <div>
                        <button type="submit">{texto}</button>

                </div>
        )
}

export default Button
